# ============================================================
# Historic Artwork Forgery Detection — Dependencies
# Python 3.10 recommended
# Install: pip install -r requirements.txt
# ============================================================

# Core data
numpy>=1.24,<2.0
pandas>=2.0,<3.0
Pillow>=10.0,<11.0
tqdm>=4.65

# ML utilities
scikit-learn>=1.3,<2.0

# Visualisation
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

# PyTorch (CNN classifier + Streamlit app)
# NOTE: For GPU support, install torch separately from https://pytorch.org/get-started/locally/
torch>=2.0,<3.0
torchvision>=0.15,<1.0

# TensorFlow (GAN pipeline only)
# NOTE: tensorflow_addons is REMOVED — it was deprecated in 2023.
#       Gaussian blur is now handled natively (see GAN/preprocess_images.py).
tensorflow>=2.13,<2.17

# Image processing
opencv-python>=4.8,<5.0

# Streamlit UI
streamlit>=1.28,<2.0

# Model persistence
joblib>=1.3,<2.0
